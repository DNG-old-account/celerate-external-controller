{{#each nodes}}
define host{
        use                     generic-host            ; Name of host template to use
        host_name               {{name}}
        alias                   {{name}}
        address                 {{ipv4}}
        }
{{/each}}

