{{#each nodes}}
{{#if ip_address}}
define host{
        use                     generic-host            ; Name of host template to use
        host_name               {{name}}
        alias                   {{name}}
        address                 {{ip_address}}
        }
{{/if}}
{{/each}}

